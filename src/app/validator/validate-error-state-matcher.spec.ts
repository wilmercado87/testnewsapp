import { ValidateErrorStateMatcher } from './validate-error-state-matcher';

describe('ValidateErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new ValidateErrorStateMatcher()).toBeTruthy();
  });
});
